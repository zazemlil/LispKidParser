# comment 1
(cdr ( quote ( (quote (A B)) (quote (C D)) ))).

# comment 2
( cons (sub 3 7) ( cdr (quote( A B C ))) ).

# comment 3
( cons (sub 3 7) ( cdr (quote( A B C ))) ).

# comment 4
(
let(del a b)
    (del 
        (lambda (x y)
            (cons 
                (dive x y) 
                (cons
                    (rem x y) 
                    (quote()) 
                )
            )
        )
    )
    (a (quote 1))
    (b (quote 2))
).