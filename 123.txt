(cdr ( quote ( (quote (A B)) (quote (C D)) ))).

( cons (sub 3 7) ( cdr (quote( A B C ))) ).

( cons (sub 3 7) ( cdr (quote( A B C ))) ).

(
let(del a b)
    (del 
        (lambda (x y)
            (cons 
                (dive x y) 
                (cons
                    (rem x y) 
                    (quote()) 
                )
            )
        )
    )
    (a (quote 1))
    (b (quote 2))
).